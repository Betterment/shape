/// The prefix to use for generated classes.
const kGeneratedClassPrefix = r'_$';

/// The suffix to use for the mixin that contains the form fields.
const kGeneratedFieldsMixinSuffix = 'Fields';

/// The name of the base class all form fields must extend.
const kFormFieldBaseClassName = 'FormField';

/// The name of the base class all form bodies must extend.
const kFormBodyBaseClassName = 'FormBody';

/// The name of the method used to validate form bodies and fields.
const kValidateMethodName = 'validate';
